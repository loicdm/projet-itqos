- hosts: "{{ host }}"
  gather_facts: no
  tasks:
    - name: "Removing shaper {{ name }} on {{ host }} interface : {{ interface }}"
      vyos.vyos.vyos_config:
        lines:
          - "delete qos interface {{ interface }} {{ direction }} {{ name }}"
          - "delete qos policy cake {{ name }}"
        save: true
